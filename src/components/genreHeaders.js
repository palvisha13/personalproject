import React from "react";

// TODO: Generalize this more so it creates headers for gernes in the givend data
import "typeface-margarine";
function genreHeaders() {
    return(
        "Horror"
    );
} export default genreHeaders;